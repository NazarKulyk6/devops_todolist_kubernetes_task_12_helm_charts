apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespaceName | default .Chart.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  clusterIP: None

