apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todoapp.configMapName" . }}
  namespace: {{ .Values.namespaceName | default .Chart.Name }}
data:
{{- range $k, $v := .Values.configMap.data }}
  {{ $k }}: {{ $v | quote }}
{{- end }}

