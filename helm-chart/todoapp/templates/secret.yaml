apiVersion: v1
kind: Secret
metadata:
  name: {{ include "todoapp.secretName" . }}
  namespace: {{ .Values.namespaceName | default .Chart.Name }}
type: Opaque
data:
{{- range $k, $v := .Values.secrets.data }}
  {{ $k }}: {{ $v | quote }}
{{- end }}

